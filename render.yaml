services:
  - type: web
    name: csitary-visual-audit-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: API_SECRET
        sync: false
      - key: ALLOWED_ORIGINS
        value: ""
